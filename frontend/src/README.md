# Frontend (React + Vite)

Struktur utama:

- `src/App.jsx`: root aplikasi (routing + layout/navigasi)
- `src/pages/`: kumpulan halaman
- `src/components/`: komponen reusable
- `src/services/`: akses API backend
- `src/context/`: state global (auth, dll)

Jalankan:
- `npm install`
- `npm run dev`
